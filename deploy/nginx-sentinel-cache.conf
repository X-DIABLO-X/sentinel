# Cache zone used only by sentinel.cypherion.tech.
# Separate file so the shared nginx.conf and other sites stay untouched.
proxy_cache_path /var/cache/nginx/sentinel levels=1:2 keys_zone=sentinel_html:16m
                 max_size=512m inactive=10m use_temp_path=off;
